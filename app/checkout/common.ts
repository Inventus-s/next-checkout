// Common Checkout Functions

export const campaignQuery = (products: string[]) => {
    // const [productId, quantity] = products
    // console.log("products",productId,quantity);
    console.log("products",products);
    
}